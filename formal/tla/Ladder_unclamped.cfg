\* Ladder.tla, the code, UNCLAMPED: all 256 raw 8-bit scalars (2 bytes of 4
\* bits, including k = 0 and odd k, so the final cswap matters), every input
\* point of Z/23 (prime) ...
SPECIFICATION Spec
CONSTANTS
    N = 23
    BYTEBITS = 4
    NBYTES = 2
    CLAMP = FALSE
    BROKEN = "none"
INVARIANTS
    TypeOK
    NoError
    SwapProtocolInv
    ResultCorrect
