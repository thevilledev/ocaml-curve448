\* BROKEN (expected to FAIL): the cswaps after the loop are omitted.  With
\* unclamped scalars, any odd k leaves the result in (x3 : z3).
SPECIFICATION Spec
CONSTANTS
    N = 23
    BYTEBITS = 4
    NBYTES = 2
    CLAMP = FALSE
    BROKEN = "nofinal"
INVARIANTS
    NoError
    ResultCorrect
