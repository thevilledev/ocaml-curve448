\* BROKEN (expected to FAIL): only the x coordinates are swapped, so a ladder
\* step receives (x2 : z2) made of coordinates of two different points.
SPECIFICATION Spec
CONSTANTS
    N = 20
    BYTEBITS = 4
    NBYTES = 2
    CLAMP = TRUE
    BROKEN = "xonly"
INVARIANTS
    NoError
    ResultCorrect
