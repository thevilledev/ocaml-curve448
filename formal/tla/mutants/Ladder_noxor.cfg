\* BROKEN (expected to FAIL): swap := k_t instead of swap ^= k_t, i.e. the
\* swap is applied but never undone.  Only the end-to-end result is checked.
SPECIFICATION Spec
CONSTANTS
    N = 20
    BYTEBITS = 4
    NBYTES = 2
    CLAMP = TRUE
    BROKEN = "noxor"
INVARIANTS
    NoError
    ResultCorrect
